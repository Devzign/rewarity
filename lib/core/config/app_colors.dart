// colors.dart
import 'package:flutter/material.dart';

/// Brand tokens (keep in one place)
class AppColors {
  // Brand
  static const green = Color(0xFF00A651);     // Brand primary
  static const orange = Color(0xFFFF9800);    // Rewards / highlights
  static const red = Color(0xFFE53935);       // Alerts / errors

  // Tiers (optional)
  static const silver   = Color(0xFFC0C0C0);
  static const gold     = Color(0xFFFFD700);
  static const platinum = Color(0xFF9E9E9E);
}
